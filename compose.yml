version: "2.6.0"

services:
  api:
   build:
     context: .
     dockerfile: ./infrastructure/Dockerfile
   restart: always
   container_name: lib-serv
   volumes:
     - ./.env:/app/.env
   network_mode: bridge
   
  nginx:
    image: nginx:latest
    restart: always
    volumes:
    - ./infrastructure/default.conf:/etc/nginx/conf.d/default.conf
    network_mode: bridge
    ports:
      - "80:80"
  postgres:
    image: postgres:13.3
    environment:
      POSTGRES_PASSWORD: "12345"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      #- ../2. Init Database:/docker-entrypoint-initdb.d
      - /var/lib/docker/volumes/testdb:/var/lib/postgresql/data
    ports:
      - "5432:5432"
  
 